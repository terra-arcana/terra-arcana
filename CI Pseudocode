create db container

create wordpress container
  install wp-cli
  start wordpress
  install wordpress
  add terra theme
  install terra theme
  activate terra theme

----------------------------------------------

download desired version of terra theme
docker run
