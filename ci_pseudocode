create db container

create wordpress container
  install wp-cli
  start wordpress
  install wordpress
  configure wordpress
  download terra theme {
    if commit hash is specified{
      git clone
      git checkout (commit hash)
    }
    else{
      git clone
      git checkout latest
    }
  }
  install terra theme
  activate terra theme
